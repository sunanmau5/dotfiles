setopt inc_append_history # share history across tmux panes/windows
setopt hist_ignore_dups # skip consecutive duplicates
setopt hist_ignore_space # skip commands prefixed with a space
setopt autocd # cd into directories without typing cd
setopt extendedglob # enable advanced glob patterns
